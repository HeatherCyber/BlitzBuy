<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blitzbuy.mapper.GoodsMapper">
<!--    字段映射：通用查询-->
    <resultMap id="BaseResultMap" type="com.example.blitzbuy.pojo.Goods">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="image_url" property="imageUrl"/>
        <result column="price" property="price"/>
        <result column="description" property="description"/>
        <result column="stock" property="stock"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

<!--    MySQL查询语句-->
    <select id="getGoodsVo" resultMap="com.example.blitzbuy.vo.GoodsVo">
        SELECT goods.id,
               goods.name,
               goods.image_url,
               goods.description,
               goods.price,
               goods.stock,
               flash_sale_goods.flash_sale_price,
               flash_sale_goods.flash_sale_stock,
               flash_sale_goods.start_time,
               flash_sale_goods.end_time
        FROM goods LEFT JOIN flash_sale_goods
            ON goods.id = flash_sale_goods.goods_id;
        </select>
</mapper>