<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blitzbuy.mapper.FlashSaleOrderMapper">

    <resultMap id="flashSaleOrderResultMap" type="com.example.blitzbuy.pojo.FlashSaleOrder">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="orderId" column="order_id"/>
        <result property="goodsId" column="goods_id"/>
    </resultMap>

    <!-- 查询秒杀订单 -->
    <!-- <select id="selectById" resultMap="flashSaleOrderResultMap">
        SELECT * FROM flash_sale_order WHERE id = #{id}
    </select> -->

    <!-- 插入秒杀订单 -->
    <!-- <insert id="insert" parameterType="com.example.blitzbuy.pojo.FlashSaleOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO flash_sale_order (user_id, order_id, goods_id)
        VALUES (#{userId}, #{orderId}, #{goodsId})
    </insert> -->

    <!-- 根据用户和商品查询秒杀订单 -->
    <!-- <select id="selectOne" resultMap="flashSaleOrderResultMap">
        SELECT * FROM flash_sale_order WHERE user_id = #{userId} AND goods_id = #{goodsId}
    </select> -->

</mapper> 